-#= render 'shared/script_levels', { script: @script, user: current_user, level: @script_level }

- if @game.video? && @script_level
  %div{ style: "float:right;"}
    = link_to "next level >>", script_level_url(@script_level.next_level)
  %iframe{ name: 'testname', src: @level.level_url, width: "100%", height: 700, frameborder: 1, scrolling: 'no', marginwidth: 0, marginheight: 0}

- if not @game.video?
  - app = @game.name.downcase
  -# Common scripts and styles
  - blockly_path = "/blockly/dist/"
  = stylesheet_link_tag blockly_path + "common.css"
  = stylesheet_link_tag blockly_path + "#{app}/style.css"

  = javascript_include_tag blockly_path + "_soy/soyutils.js"
  = javascript_include_tag blockly_path + "#{app}/generated/en_us.js"

  = javascript_include_tag blockly_path + "blockly_compressed.js"
  = javascript_include_tag blockly_path + "javascript_compressed.js"
  = javascript_include_tag blockly_path + "en_compressed.js"

  = javascript_include_tag blockly_path + "#{app}/#{app}.js"
  :javascript
    #{app}Main();


-##level_area
-#
-#:javascript
-#  $.ajax({
-#    type: "GET",
-#    url: '#{level_url}',
-#    //data: "id=" + id, // appears as $_GET['id'] @ ur backend side
-#    success: function(data) {
-#        // data is ur summary
-#        $('#level_area').html(data);
-#    }});

- if can? :edit, @level
  %br/
  = link_to '(edit)', edit_game_level_path(@game, @level)

