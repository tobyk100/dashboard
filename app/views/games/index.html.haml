%h1 Listing games
%table
  %thead
    %tr
      %th{ width:100 } Game
      %th{ width:100 } Levels
      %th{ width:100 }
      %th{ width:100 }
  %tbody
    - @games.each do |game|
      %tr
        %td= link_to game.name, game
        %td= game.levels.count
        - if can? :update, game
          %td= link_to 'Edit', edit_game_path(game)
        - if can? :destroy, game
          %td= link_to 'Destroy', game, method: :delete, data: { confirm: 'Are you sure?' }
%br/
- if can? :create, Game
  = link_to 'New Game', new_game_path
